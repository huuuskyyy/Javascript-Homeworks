Create a jQuery plugin for creating dropdown list. And make it work as SELECT node
 Selecting an one of the generated LI nodes, selects the
corresponding OPTION node
 So $('#dropdown:selected') works
